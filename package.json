{
  "name": "hopverkefni2",
  "version": "1.0.0",
  "description": "Hópverkefni 2  Carlos og Ragnarok",
  "main": "index.js",
  "scripts": {
    "compile:sass": "sass styles/config.scss styles.css",
    "watch:sass": "sass --watch styles/config.scss styles.css",
    "browser-sync": "browser-sync start --server --files 'sidur/*.html, *.css'",
    "lint:style": "stylelint 'styles/**/*.scss'",
    "dev": "concurrently \"npm run watch:sass\" \"npm run browser-sync\"",
    "test": "echo \"Error: no test specified\" && exit 1"
    
  },
  "repository": {
    "type": "git",
    "url": "hopverkefni2"
  },
  "author": "Karl og Ragnar",
  "license": "ISC",
  "devDependencies": {
    "browser-sync": "^2.29.3",
    "concurrently": "^8.2.2",
    "cross-env": "^7.0.3",
    "install": "^0.13.0",
    "npm": "^10.2.4",
    "sass": "^1.69.4",
    "stylelint": "^15.11.0"
  }
}
